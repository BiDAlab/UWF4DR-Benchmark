# Requirements

This directory contains Python dependency files for different model families
used in the experiments.

- `cnn.txt`: environments for convolutional neural networks
- `vit.txt`: environments for vision transformer models
- `retfound.txt`: environments for RETFound-based models
